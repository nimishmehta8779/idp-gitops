# Well-Architected Review — three-tier-web

Generated by aws-architect skill.
Source checklist: `infrastructure/patterns/_lib/well-architected-checklist.yaml`

| Pillar | ID | Practice | Status | Evidence | Notes |
|---|---|---|---|---|---|
| SEC | SEC02-BP02 | Use temporary credentials | Pass | `ecs-task-role` and `ecs-task-execution-role` use `sts:AssumeRole` scoped to `ecs-tasks.amazonaws.com`; no long-lived keys in composition | |
| SEC | SEC03-BP02 | Grant least privilege access | Pass | Task execution role uses `AmazonECSTaskExecutionRolePolicy` (managed, scoped); task role is empty by default for teams to extend; no `*` actions | |
| SEC | SEC05-BP01 | Create network layers | Pass | Three distinct SGs: `sg-alb` (public), `sg-app` (ingress from ALB SG), `sg-data` (ingress port 5432) | |
| SEC | SEC05-BP02 | Control traffic flow within your network layers | Pass | ALB SG: 0.0.0.0/0 on 80+443 only. App SG: ingress from ALB SG. Data SG: 5432 from app SG. All egress minimal. | |
| SEC | SEC08-BP01 | Implement secure key management | Partial | RDS uses `storageEncrypted: true` (AWS-managed key). CMK not enforced — acceptable for beta; add KMS key ref for prod hardening. | Add CMK parameter in future revision |
| SEC | SEC08-BP02 | Enforce encryption at rest | Pass | RDS: `storageEncrypted: true`. No S3/DynamoDB in this pattern — N/A for those. | |
| SEC | SEC09-BP02 | Enforce encryption in transit | Pass | ALB: HTTPS listener :443 present; HTTP :80 redirects to HTTPS :443 (`HTTP_301`). RDS: `publiclyAccessible: false`. | |
| REL | REL09-BP01 | Identify and back up all data that needs to be backed up | Pass | RDS `backupRetentionPeriod` transform: dev=0, staging=7. Prod would be 30 (prod env not yet in enum). | Add `prod` to enum when prod env supported |
| REL | REL09-BP02 | Secure and encrypt backups | Pass | RDS `storageEncrypted: true` covers automated snapshots. | |
| REL | REL09-BP03 | Perform data backup automatically | Pass | `autoMinorVersionUpgrade: true` set. Backup retention >0 for staging triggers automated snapshots. | |
| REL | REL10-BP01 | Deploy the workload to multiple locations | Pass | RDS `multiAz` transform: dev=false, staging=true. ALB spans public subnets (multi-AZ by selector). ECS spans private subnets. | |
| REL | REL11-BP01 | Monitor all components of the workload to detect failures | Pass | ECS cluster: `containerInsights: enabled`. CloudWatch log group provisioned. ALB target group has health check configured. | |
| REL | REL11-BP02 | Fail over to healthy resources | Pass | ALB target group health check: `healthyThreshold: 3`, `unhealthyThreshold: 3`, `intervalSeconds: 30`. | Consider requiring `desiredCount >= 2` for staging in XRD |
| PERF | PERF01-BP03 | Factor cost into architectural decisions | Pass | Dev uses `db.t3.medium` default, `desiredCount: 1`, `ecsTaskCpu: 512`. Transform maps enforce lighter sizing in dev. | |
| PERF | PERF01-BP07 | Use a data-driven approach for architectural choices | Pass | XRD exposes `ecsTaskCpu`, `ecsTaskMemory`, `dbInstanceClass` as explicit parameters; teams right-size from metrics. | |
| COST | COST03-BP01 | Configure detailed information sources | Pass | All resources carry `team`, `environment`, `cost-center`, `managed-by`, `provisioned-by` tags via patch. | |
| COST | COST03-BP02 | Add organization information to cost and usage | Pass | `cost-center` patched from `spec.parameters.costCenter`; `team` from `spec.parameters.teamName`. Neither is hardcoded. | |
| COST | COST06-BP02 | Select resource type, size, and number based on data | Pass | `dbInstanceClass` XRD enum; `ecsTaskCpu`/`ecsTaskMemory` as Fargate-valid enums; `desiredCount` with min/max bounds. | |
| COST | COST07-BP04 | Implement pricing models for all components | Pass | Fargate pricing model (no EC2 reservation complexity). Dev/staging sizing separated via transform maps. | |
| OPS | OPS04-BP01 | Implement application telemetry | Pass | CloudWatch log group: `/ecs/<appName>-<env>` with retention transform (dev=7d, staging=30d). ECS containerInsights enabled. | |
| OPS | OPS05-BP04 | Use version control | Pass | `idp.platform.io/pattern-version: "1.0.0"` annotation on both XRD and Composition. GitOps via ArgoCD. | |
| SUS | SUS05-BP03 | Use managed services | Pass | ECS Fargate (no EC2 to manage). RDS managed PostgreSQL. Hardware efficiency optimization delegated to AWS. | |
| SUS | SUS05-BP04 | Optimize your use of GPUs | N/A | No GPU workloads in three-tier-web pattern. | |
